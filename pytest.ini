[pytest]
testpaths = tests
asyncio_mode = auto
addopts = --cov=src --cov-report=term-missing --cov-fail-under=35
markers =
    integration: marks tests as integration (deselect with '-m "not integration"')
    slow: marks tests as slow-running
